import React from 'react'

const PrefixInput = ({children}) => {
  return (
    <div className='ballena-prefix-input'>
      {children}
    </div>
  )
}

PrefixInput.id = 'PrefixInput';

export default PrefixInput
